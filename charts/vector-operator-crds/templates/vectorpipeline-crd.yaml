{{- if .Values.createCRDs }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: vectorpipelines.vector.zcentric.com
spec:
  group: vector.zcentric.com
  names:
    kind: VectorPipeline
    listKind: VectorPipelineList
    plural: vectorpipelines
    singular: vectorpipeline
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.vectorRef
      name: VectorRef
      type: string
    - jsonPath: .status.conditions[?(@.type=="VectorRefValid")].status
      name: Status
      type: string
    - jsonPath: .status.conditions[?(@.type=="VectorRefValid")].message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VectorPipeline is the Schema for the vectorpipelines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VectorPipelineSpec defines the desired state of VectorPipeline
            properties:
              sinks:
                additionalProperties:
                  description: Sink defines a Vector sink configuration
                  properties:
                    config:
                      description: Config contains the sink-specific configuration
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    encoding:
                      description: Encoding specifies the sink encoding configuration
                      properties:
                        codec:
                          description: Codec specifies the encoding codec
                          type: string
                      required:
                      - codec
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    inputs:
                      description: Inputs specifies the input sources or transforms
                      items:
                        type: string
                      minItems: 1
                      type: array
                    type:
                      description: Type specifies the type of the sink
                      type: string
                  required:
                  - inputs
                  - type
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Sinks defines the Vector sinks configuration
                type: object
                x-kubernetes-preserve-unknown-fields: true
              sources:
                additionalProperties:
                  description: Source defines a Vector source configuration
                  properties:
                    config:
                      description: Config contains the source-specific configuration
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    extra_label_selector:
                      description: ExtraLabelSelector specifies additional label selectors for kubernetes_logs source
                      type: string
                    type:
                      description: Type specifies the type of the source
                      type: string
                  required:
                  - type
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Sources defines the Vector sources configuration
                type: object
                x-kubernetes-preserve-unknown-fields: true
              transforms:
                additionalProperties:
                  description: Transform defines a Vector transform configuration
                  properties:
                    condition:
                      description: Condition specifies the filter condition (for filter transform)
                      properties:
                        source:
                          description: Source contains the condition expression
                          type: string
                        type:
                          description: Type specifies the condition type
                          type: string
                      required:
                      - source
                      - type
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    config:
                      description: Config contains the transform-specific configuration
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    inputs:
                      description: Inputs specifies the input sources or transforms
                      items:
                        type: string
                      minItems: 1
                      type: array
                    source:
                      description: Source contains the transform expression (for remap transform)
                      type: string
                    type:
                      description: Type specifies the type of the transform
                      type: string
                  required:
                  - inputs
                  - type
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: Transforms defines the Vector transforms configuration
                type: object
                x-kubernetes-preserve-unknown-fields: true
              vectorRef:
                description: VectorRef references the Vector CR to use
                type: string
            required:
            - vectorRef
            type: object
          status:
            description: VectorPipelineStatus defines the observed state of VectorPipeline
            properties:
              conditions:
                description: Conditions represent the latest available observations of VectorPipeline's state
                items:
                  description: "Condition contains details for one aspect of the current state of this API Resource."
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
